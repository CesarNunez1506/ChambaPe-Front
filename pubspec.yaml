name: chamba_app
description: A new Flutter project connecting service providers with clients in Peru.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: # Add this for date/time formatting in Spanish
    sdk: flutter         #
  cupertino_icons: ^1.0.6
  provider: ^6.1.1 # For state management
  intl: ^0.18.1 # For date/time/number formatting (confirm latest version)
  google_fonts: ^6.1.0 # For custom fonts like Montserrat
  # image_picker: ^1.0.0 # For picking images (e.g., profile photos) - keep commented if not used yet
  # geolocator: ^10.0.0 # For location services - keep commented if not used yet
  # url_launcher: ^6.1.10 # For launching URLs (maps, phone) - keep commented if not used yet


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # assets:
  #   - assets/images/logo.png
  #   - assets/images/profile_placeholder.png
  #   - assets/fonts/ # If you were to manually add Montserrat fonts

  # fonts: # Using google_fonts package is generally easier than manual setup
  #   - family: Montserrat
  #     fonts:
  #       - asset: assets/fonts/Montserrat-Regular.ttf
  #       - asset: assets/fonts/Montserrat-Italic.ttf
  #         style: italic
  #       - asset: assets/fonts/Montserrat-Bold.ttf
  #         weight: 700
  #       - asset: assets/fonts/Montserrat-SemiBold.ttf
  #         weight: 600
